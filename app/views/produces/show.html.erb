<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @produce.name %>
</p>

<p>
  <strong>Duration:</strong>
  <%= @produce.duration %>
</p>

<p>
  <strong>Produce:</strong>
    <%= @produce.GardenProduces.each do |prod| %>
  <% end %> %>
</p>

<%= link_to 'Edit', edit_produce_path(@produce) %> |
<%= link_to 'Back', produces_path %>
