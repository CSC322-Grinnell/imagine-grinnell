<%= form_for(@produce) do |f| %>
  <% if @produce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produce.errors.count, "error") %> prohibited this produce from being saved:</h2>

      <ul>
      <% @produce.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :availability_prediction %><br>
    <%= f.text_field :availability_prediction %>
  </div>
  <div class="field">
    <%= f.label :readiness %><br>
    <%= f.number_field :readiness %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
